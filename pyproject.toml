[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "maze_generator"
version = "0.1.0"
description = "Un motor generador de laberintos en Python."
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]